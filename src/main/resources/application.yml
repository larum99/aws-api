server:
  port: 8080

spring:
  application:
    name: aws-api
  datasource:
    url: jdbc:mysql://aws-api.ckbckiw04402.us-east-1.rds.amazonaws.com/aws_api
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update